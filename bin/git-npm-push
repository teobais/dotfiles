#!/bin/sh

# Updates and pushed the updated version to npm and git (master branch)
# Future thoughts: provide arguments for version (major, minor, patch) and branch

npm version patch
npm publish
git push origin master
